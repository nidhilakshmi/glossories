<%= form_with(model: synonym) do |form| %>
  <% if synonym.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(synonym.errors.count, "error") %> prohibited this synonym from being saved:</h2>

      <ul>
        <% synonym.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :english_word %>
    <%= form.text_field :english_word %>
  </div>

  <div class="field">
    <%= form.label :kannada_word %>
    <%= form.text_field :kannada_word %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
